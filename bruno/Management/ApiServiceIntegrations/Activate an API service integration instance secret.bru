meta {
  name: Activate an API service integration instance secret
  type: http
  seq: 8
}

post {
  url: {{orgUrl}}/integrations/api/v1/api-services/{{apiServiceId}}/credentials/secrets/{{secretId}}/lifecycle/activate
  body: none
  auth: none
}

headers {
  Accept: application/json
  Authorization: SSWS {{apiKey}}
}

docs {
Activates an API Service Integration instance Secret by `secretId`

**Responses:**
- `200`: OK
- `401`: 
- `403`: 
- `404`: 
- `429`:
}
