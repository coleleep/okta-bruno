meta {
  name: Retrieve an application token
  type: http
  seq: 3
}

get {
  url: {{orgUrl}}/api/v1/apps/{{appId}}/tokens/{{tokenId}}
  body: none
  auth: none
}

headers {
  Accept: application/json
  Authorization: SSWS {{apiKey}}
}

docs {
Retrieves a refresh token for the specified app

**Responses:**
- `200`: Success
- `403`: 
- `404`: 
- `429`:
}
