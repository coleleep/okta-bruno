meta {
  name: Revoke a refresh token for a client
  type: http
  seq: 5
}

delete {
  url: {{orgUrl}}/api/v1/authorizationServers/{{authServerId}}/clients/{{clientId}}/tokens/{{tokenId}}
  body: none
  auth: none
}

headers {
  Accept: application/json
  Authorization: SSWS {{apiKey}}
}

docs {
Revokes a refresh token for a Client

**Responses:**
- `204`: No Content
- `403`: 
- `404`: 
- `429`:
}
