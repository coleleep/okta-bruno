meta {
  name: Retrieve the email template settings
  type: http
  seq: 12
}

get {
  url: {{orgUrl}}/api/v1/brands/{{brandId}}/templates/email/{{templateName}}/settings
  body: none
  auth: none
}

headers {
  Accept: application/json
  Authorization: SSWS {{apiKey}}
}

docs {
Retrieves an email template's settings

**Responses:**
- `200`: Successfully retrieved the email template's settings.
- `403`: 
- `404`: 
- `429`:
}
