meta {
  name: List all certificate signing requests
  type: http
  seq: 1
}

get {
  url: {{orgUrl}}/api/v1/idps/{{idpId}}/credentials/csrs
  body: none
  auth: none
}

headers {
  Accept: application/json
  Authorization: SSWS {{apiKey}}
}

docs {
Lists all certificate signing requests (CSRs) for an identity provider (IdP)

**Responses:**
- `200`: Success
- `403`: 
- `404`: 
- `429`:
}
