meta {
  name: List all signing key credentials for IdP
  type: http
  seq: 6
}

get {
  url: {{orgUrl}}/api/v1/idps/{{idpId}}/credentials/keys
  body: none
  auth: none
}

headers {
  Accept: application/json
  Authorization: SSWS {{apiKey}}
}

docs {
Lists all signing key credentials for an identity provider (IdP)

**Responses:**
- `200`: Success
- `403`: 
- `404`: 
- `429`:
}
