meta {
  name: List the active signing key credential for IdP
  type: http
  seq: 7
}

get {
  url: {{orgUrl}}/api/v1/idps/{{idpId}}/credentials/keys/active
  body: none
  auth: none
}

headers {
  Accept: application/json
  Authorization: SSWS {{apiKey}}
}

docs {
Lists the active signing key credential for an identity provider (IdP)

**Responses:**
- `200`: Success
- `204`: No Content
- `429`:
}
