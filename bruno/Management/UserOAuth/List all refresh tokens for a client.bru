meta {
  name: List all refresh tokens for a client
  type: http
  seq: 1
}

get {
  url: {{orgUrl}}/api/v1/users/{{userId}}/clients/{{clientId}}/tokens
  body: none
  auth: none
}

params:query {
  ~expand: 
  ~after: 
  ~limit: 20
}

headers {
  Accept: application/json
  Authorization: SSWS {{apiKey}}
}

docs {
Lists all refresh tokens issued for the specified user and client

**Responses:**
- `200`: Success
- `403`: 
- `404`: 
- `429`:
}
