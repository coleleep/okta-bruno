meta {
  name: Retrieve a custom token claim
  type: http
  seq: 3
}

get {
  url: {{orgUrl}}/api/v1/authorizationServers/{{authServerId}}/claims/{{claimId}}
  body: none
  auth: none
}

headers {
  Accept: application/json
  Authorization: SSWS {{apiKey}}
}

docs {
Retrieves a custom token Claim by the specified `claimId`

**Responses:**
- `200`: Success
- `403`: 
- `404`: 
- `429`:
}
