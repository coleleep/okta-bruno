meta {
  name: Retrieve a client role
  type: http
  seq: 3
}

get {
  url: {{orgUrl}}/oauth2/v1/clients/{{clientId}}/roles/{{roleAssignmentId}}
  body: none
  auth: none
}

headers {
  Accept: application/json
  Authorization: SSWS {{apiKey}}
}

docs {
Retrieves a role assignment (identified by `roleAssignmentId`) for a client app (identified by `clientId`)

**Responses:**
- `200`: Success
- `403`: 
- `404`: 
- `429`:
}
