meta {
  name: Retrieve the user role governance source resources
  type: http
  seq: 8
}

get {
  url: {{orgUrl}}/api/v1/users/{{userId}}/roles/{{roleAssignmentId}}/governance/{{grantId}}/resources
  body: none
  auth: none
}

headers {
  Accept: application/json
  Authorization: SSWS {{apiKey}}
}

docs {
Retrieves the resources of a governance source (identified by `grantId`) for a role (identified by `roleAssignmentId`) that's assigned to a user (identified by `userId`)

**Responses:**
- `200`: Success
- `403`: 
- `404`: 
- `429`:
}
