meta {
  name: Retrieve a user type
  type: http
  seq: 3
}

get {
  url: {{orgUrl}}/api/v1/meta/types/user/{{typeId}}
  body: none
  auth: none
}

headers {
  Accept: application/json
  Authorization: SSWS {{apiKey}}
}

docs {
Retrieves a user type by ID. Use `default` to fetch the default user type.

**Responses:**
- `200`: Success
- `403`: 
- `404`: 
- `429`:
}
