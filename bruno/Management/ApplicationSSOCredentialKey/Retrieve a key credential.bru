meta {
  name: Retrieve a key credential
  type: http
  seq: 8
}

get {
  url: {{orgUrl}}/api/v1/apps/{{appId}}/credentials/keys/{{keyId}}
  body: none
  auth: none
}

headers {
  Accept: application/json
  Authorization: SSWS {{apiKey}}
}

docs {
Retrieves a specific application key credential by `kid`

**Responses:**
- `200`: Success
- `403`: 
- `404`: 
- `429`:
}
