meta {
  name: List all credential keys
  type: http
  seq: 1
}

get {
  url: {{orgUrl}}/api/v1/authorizationServers/{{authServerId}}/credentials/keys
  body: none
  auth: none
}

headers {
  Accept: application/json
  Authorization: SSWS {{apiKey}}
}

docs {
Lists all of the current, future, and expired Keys used by the Custom Authorization Server

**Responses:**
- `200`: Success
- `403`: 
- `404`: 
- `429`:
}
