meta {
  name: Create an identity source user
  type: http
  seq: 19
}

post {
  url: {{orgUrl}}/api/v1/identity-sources/{{identitySourceId}}/users
  body: json
  auth: none
}

headers {
  Accept: application/json
  Content-Type: application/json
  Authorization: SSWS {{apiKey}}
}

body:json {
  {
    "externalId": ""
  }
}

docs {
Creates a user in an identity source for the given identity source instance

**Responses:**
- `200`: Success
- `403`: 
- `404`: 
- `429`:
}
