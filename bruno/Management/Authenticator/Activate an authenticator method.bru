meta {
  name: Activate an authenticator method
  type: http
  seq: 17
}

post {
  url: {{orgUrl}}/api/v1/authenticators/{{authenticatorId}}/methods/{{methodType}}/lifecycle/activate
  body: none
  auth: none
}

headers {
  Accept: application/json
  Authorization: SSWS {{apiKey}}
}

docs {
Activates a method for an authenticator identified by `authenticatorId` and `methodType`

**Responses:**
- `200`: Success
- `403`: 
- `404`: 
- `429`:
}
