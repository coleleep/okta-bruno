meta {
  name: Activate an authenticator
  type: http
  seq: 12
}

post {
  url: {{orgUrl}}/api/v1/authenticators/{{authenticatorId}}/lifecycle/activate
  body: none
  auth: none
}

headers {
  Accept: application/json
  Authorization: SSWS {{apiKey}}
}

docs {
Activates an authenticator by `authenticatorId`

**Responses:**
- `200`: 
- `403`: 
- `404`: 
- `429`:
}
